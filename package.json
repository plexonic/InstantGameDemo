{
  "name": "phaser-1",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build:prod": "webpack --env prod",
    "build:dev": "webpack --env dev",
    "test": "yarn run build:prod && yarn run gulp test",
    "mock": "yarn run build:dev && yarn run gulp mock",
    "mock:w/o build": "gulp mock",
    "push": "yarn run build:prod && yarn run gulp push"
  },
  "dependencies": {
    "phaser": "^3.11.0"
  },
  "devDependencies": {
    "@types/facebook-instant-games": "^6.2.1",
    "gulp": "3.9.1",
    "gulp-html-replace": "^1.6.2",
    "gulp-webserver": "^0.9.1",
    "gulp-zip": "^4.2.0",
    "open": "^0.0.5",
    "request": "^2.88.0",
    "ts-loader": "^4.5.0",
    "ts-node": "^7.0.1",
    "typescript": "^3.0.1",
    "uglifyjs-webpack-plugin": "^1.3.0",
    "uuid": "latest",
    "webpack": "^4.17.0",
    "webpack-cli": "^3.1.0",
    "webpack-dev-server": "^3.1.5"
  }
}
